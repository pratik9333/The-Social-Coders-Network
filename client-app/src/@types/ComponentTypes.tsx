type LandingButtonProps = {
    className ?:string | undefined,
    content ?:string  | undefined
}

type NavButtonType = {
    className ?:string | undefined,
    content ?:string | undefined,
    location ?: any
}

export type { LandingButtonProps , NavButtonType}